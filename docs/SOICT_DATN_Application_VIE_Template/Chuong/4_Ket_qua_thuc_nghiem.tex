\documentclass[../DoAn.tex]{subfiles}
\begin{document}

Chương 3 đã trình bày nền tảng lý thuyết và các công nghệ được lựa chọn cho từng tầng trong kiến trúc hệ thống. Trên cơ sở đó, Chương 4 trình bày chi tiết quá trình thiết kế, triển khai và đánh giá hệ thống. Nội dung chương bao gồm thiết kế kiến trúc (phần~\ref{section:4.1}), thiết kế chi tiết (phần~\ref{section:4.2}), xây dựng ứng dụng (phần~\ref{section:4.3}), kiểm thử (phần~\ref{section:4.4}), và triển khai (phần~\ref{section:4.5}).

\section{Thiết kế kiến trúc}
\label{section:4.1}

\subsection{Lựa chọn kiến trúc phần mềm}
\label{subsection:4.1.1}

Hệ thống được thiết kế theo kiến trúc bốn tầng, bao gồm tầng cảm biến (Sensor Layer), tầng trung chuyển (Gateway Layer), tầng máy chủ (Server Layer) và tầng hiển thị (Presentation Layer). Kiến trúc bốn tầng được lựa chọn vì ba lý do chính. Thứ nhất, mỗi tầng có trách nhiệm rõ ràng và có thể phát triển độc lập: tầng cảm biến thu thập dữ liệu môi trường, tầng trung chuyển chuyển tiếp dữ liệu từ mạng \gls{LoRa} sang Internet, tầng máy chủ xử lý nghiệp vụ và lưu trữ, tầng hiển thị cung cấp giao diện cho người dùng. Thứ hai, kiến trúc này cho phép mở rộng theo chiều ngang tại từng tầng — ví dụ thêm Sensor Node mới mà không thay đổi phần mềm Backend. Thứ ba, độ phức tạp phù hợp với quy mô hệ thống: triển khai trên một máy chủ VPS duy nhất, không yêu cầu kiến trúc Microservice phức tạp.

\textbf{Tầng cảm biến} gồm các Sensor Node, mỗi node tích hợp vi điều khiển ESP32, ba module cảm biến (PMS7003, CCS811, AHT10) và module \gls{LoRa} AS32-TTL-100. Firmware được thiết kế tối ưu điện năng bằng cơ chế Deep-sleep: thiết bị thức dậy theo chu kỳ để đọc cảm biến và gửi LoRa, chỉ hiển thị OLED khi người dùng nhấn nút. Dữ liệu được đóng gói thành gói tin nhị phân 18 byte và phát qua \gls{LoRa} 433 MHz.

\textbf{Tầng trung chuyển} gồm các Gateway, mỗi Gateway tích hợp ESP32 với module \gls{LoRa} và kết nối WiFi. Gateway nhận gói tin \gls{LoRa}, lưu vào bộ đệm vòng (ring buffer), sau đó gửi lên tầng máy chủ dưới dạng \gls{HTTP} POST (\gls{JSON} batch). Ở tầng này, hệ thống áp dụng mô hình Hub-Spoke: nhiều Sensor Node (spoke) gửi dữ liệu đến một Gateway (hub) trung tâm, cho phép một Gateway phục vụ nhiều Sensor Node trong phạm vi phủ sóng mà không cần định tuyến phức tạp.

\textbf{Tầng máy chủ} bao gồm Backend Server (Node.js/Express) và cơ sở dữ liệu (PostgreSQL với TimescaleDB và PostGIS), được đóng gói trong Docker container. Backend Server được tổ chức theo mô hình phân lớp gồm ba lớp: lớp Controller tiếp nhận \gls{HTTP} request, lớp Service chứa logic nghiệp vụ, và lớp Data Access tương tác với cơ sở dữ liệu thông qua Prisma \gls{ORM}. Ngoài ra, tầng máy chủ còn có Nginx đóng vai trò reverse proxy và phục vụ tệp tĩnh, cùng các tác vụ nền (cron job) kiểm tra thiết bị offline và dọn dẹp dữ liệu cũ.

\textbf{Tầng hiển thị} là ứng dụng web \gls{SPA} xây dựng bằng React 19 và Vite 8, giao tiếp với tầng máy chủ qua \gls{REST} \gls{API} (\gls{HTTP}/HTTPS) và Socket.IO (WebSocket). Tầng này cung cấp hai nhóm giao diện: dashboard cho người dùng (bản đồ AQI, biểu đồ lịch sử, xếp hạng) và trang quản trị cho quản trị viên (quản lý thiết bị, cảnh báo, cấu hình, xuất dữ liệu).

\subsection{Thiết kế tổng quan}
\label{subsection:4.1.2}

\begin{figure}[h]
    \centering
    \includegraphics[width=1.00\textwidth]{Hinhve/package_tongquan.png}
    \caption{Biểu đồ gói UML tổng quan của hệ thống}
    \label{fig:4_1_2_package_diagram}
\end{figure}

Hệ thống được tổ chức thành bốn nhóm gói chính tương ứng với bốn tầng kiến trúc, mỗi nhóm chứa các gói con với trách nhiệm cụ thể.

\textbf{Nhóm gói Tầng cảm biến (sensor-node)} chứa các module: \texttt{drivers} (giao tiếp cảm biến PMS7003, CCS811, AHT10, màn hình OLED, module LoRa), \texttt{core} (quản lý cấu hình NVS, provisioning qua WiFi AP, quản lý chu kỳ deep-sleep), và \texttt{config} (định nghĩa chân GPIO, tham số LoRa).

\textbf{Nhóm gói Tầng trung chuyển (gateway)} chứa các module: \texttt{drivers} (giao tiếp LoRa), \texttt{core} (bộ đệm vòng, gửi \gls{HTTP} batch lên tầng máy chủ), và \texttt{config} (cấu hình WiFi, địa chỉ Backend). Nhóm gói này hoạt động độc lập, không phụ thuộc vào nhóm gói tầng cảm biến.

\textbf{Nhóm gói Tầng máy chủ (backend)} gồm bốn gói chính: \texttt{routes} (định nghĩa endpoint API), \texttt{controllers} (tiếp nhận request, trả response), \texttt{services} (logic nghiệp vụ), và \texttt{config} (kết nối Prisma Client và cấu hình hệ thống). Luồng phụ thuộc tuân thủ nguyên tắc một chiều: \texttt{routes} $\rightarrow$ \texttt{controllers} $\rightarrow$ \texttt{services} $\rightarrow$ \texttt{config}, đảm bảo gói tầng dưới không phụ thuộc ngược lại gói tầng trên.

\textbf{Nhóm gói Tầng hiển thị (frontend)} gồm bốn gói chính: \texttt{pages} (các trang giao diện), \texttt{components} (thành phần tái sử dụng), \texttt{hooks} (custom React hooks để nạp dữ liệu và xử lý logic), và \texttt{services} (giao tiếp API). Mô hình phụ thuộc được thiết kế rõ ràng: gói \texttt{pages} phụ thuộc vào \texttt{components} và \texttt{hooks}; gói \texttt{hooks} phụ thuộc vào \texttt{services}; gói \texttt{services} chịu trách nhiệm gọi \gls{REST} \gls{API} và giao tiếp Socket.IO đến Tầng máy chủ.

\subsection{Mô tả luồng dữ liệu giữa các gói}
\label{subsection:4.1.3}

\begin{figure}[h]
    \centering
    \includegraphics[width=1.00\textwidth]{Hinhve/module_backend_services.png}
    \caption{Biểu đồ quan hệ module cho backend services}
    \label{fig:4_1_3_module_backend_services}
\end{figure}


Hình~\ref{fig:4_1_3_module_backend_services} mô tả quan hệ phụ thuộc giữa các module trên Backend. Hai nhóm module quan trọng nhất là nhóm xử lý dữ liệu telemetry và nhóm hiển thị dashboard.

\textbf{Nhóm xử lý dữ liệu telemetry} bao gồm bốn module: \texttt{telemetryController} tiếp nhận request, \texttt{telemetryValidation} xác thực đầu vào, \texttt{telemetryService} xử lý nghiệp vụ (deduplication, lưu trữ, kiểm tra cảnh báo), và \texttt{aqiService} tính chỉ số AQI. Luồng xử lý chi tiết được trình bày qua ba biểu đồ trình tự ở phần~\ref{subsection:4.2.4}.

\textbf{Nhóm hiển thị dashboard} trên Frontend sử dụng hook \texttt{useDashboard} (gọi API lấy dữ liệu ban đầu) và hook \texttt{useSocket} (nhận cập nhật thời gian thực qua Socket.IO). Chi tiết luồng end-to-end từ truy cập Dashboard đến cập nhật realtime được mô tả ở Hình~\ref{fig:seq_dashboard}.

\section{Thiết kế chi tiết}
\label{section:4.2}

\subsection{Thiết kế phần cứng}
\label{subsection:4.2.1}

Phần này trình bày thiết kế mạch kết nối phần cứng cho Sensor Node và Gateway.

\textbf{Sensor Node} sử dụng board ESP32 DevKit V1 (30 chân) làm trung tâm, kết nối với bốn module ngoại vi qua ba giao tiếp khác nhau. Module \gls{LoRa} AS32-TTL-100 giao tiếp qua \gls{UART}1 (RX: GPIO16, TX: GPIO17) với hai chân điều khiển chế độ (MD0: GPIO4, MD1: GPIO5). Module cảm biến bụi PMS7003 giao tiếp qua \gls{UART}2 (RX: GPIO25, TX: GPIO26). Module CCS811 và AHT10 chia sẻ chung bus I2C (SDA: GPIO21, SCL: GPIO22) với địa chỉ lần lượt là 0x5A và 0x38; CCS811 có thêm chân WAK (GPIO23) để đánh thức từ chế độ ngủ. Màn hình OLED SSD1306 cũng dùng chung bus I2C trên, địa chỉ 0x3C. Pin 18650 được đo qua mạch phân áp (voltage divider R1=R2=100K$\Omega$) nối vào GPIO15 (ADC). Nút BOOT (GPIO0) được tận dụng làm nút factory reset: giữ 5 giây để xóa cấu hình và vào chế độ provisioning.

\begin{figure}[h]
    \centering
    \includegraphics[width=1.00\textwidth]{Hinhve/schematic_sensor_node.png}
    \caption{Sơ đồ mạch Sensor Node}
    \label{fig:4_2_1_schematic_sensor_node}
\end{figure}

    

\textbf{Gateway} sử dụng cùng board ESP32 DevKit V1, nhưng chỉ kết nối một module ngoại vi: module \gls{LoRa} AS32-TTL-100 qua \gls{UART}1 (cùng cấu hình chân với Sensor Node). Gateway sử dụng màn hình OLED SSD1306 để hiển thị trạng thái hoạt động và các thông tin cần thiết.

\begin{figure}[h]
    \centering
    \includegraphics[width=1.00\textwidth]{Hinhve/schematic_gateway.png}
    \caption{Sơ đồ mạch Gateway}
    \label{fig:4_2_2_schematic_gateway}
\end{figure}

\subsection{Thiết kế firmware}
\label{subsection:4.2.2}

Phần này trình bày thiết kế firmware cho Sensor Node và Gateway.

\textbf{Firmware Sensor Node} được thiết kế tối ưu năng lượng dựa trên cơ chế Deep-sleep của hệ điều hành quản lý ESP32. Thay vì chạy vòng lặp liên tục, vi điều khiển chỉ thức dậy theo chu kỳ hẹn giờ Timer hoặc qua ngắt bằng nút cứng (nút BOOT). Khi được đánh thức bởi Timer, firmware thực hiện tuần tự các tác vụ khởi tạo phần cứng, gọi hệ thống lấy mẫu cảm biến, chờ cấp nhiệt (warm-up) cảm biến bụi thư viện, lấy mẫu pin qua ADC, đóng gói tải trọng dữ liệu gửi qua LoRa, và cuối cùng đối chiếu lưu trữ lại kết quả vừa gửi vào vùng nhớ RTC trước khi quay về lại trạng thái Deep-sleep. Ngược lại, nếu được đánh thức bởi người dùng, thiết bị bỏ qua quá trình lấy mẫu, mà truy cập vùng nhớ RTC để hiển thị dữ liệu lịch sử ngay lên màn hình OLED trong vòng 10 giây trước khi tiếp tục tắt hiển thị. Kiến trúc này giúp ngắt triệt để điện năng của các module ngoại vi, triệt tiêu hao phí thừa so với cơ chế FreeRTOS thông thường.

Cấu trúc gói tin \gls{LoRa} được định nghĩa dưới dạng struct C packed (không có padding), tổng kích thước 18 byte. Gói tin gồm: \texttt{nodeId} (1 byte, ID node 1--255), \texttt{pktType} (1 byte, phân biệt gói dữ liệu/heartbeat/lỗi), \texttt{msgId} (1 byte, bộ đếm tuần hoàn 0--255 phục vụ deduplication), bảy trường dữ liệu cảm biến (PM1, PM2.5, PM10 mỗi trường 2 byte nhân 10, CO\textsubscript{2} và TVOC mỗi trường 2 byte, nhiệt độ 2 byte có dấu nhân 10, độ ẩm 2 byte nhân 10), và \texttt{battery} (1 byte, 0--100\%). Các giá trị dạng số thực được nhân 10 và lưu dạng số nguyên để tránh sử dụng floating-point trong truyền thông, giảm kích thước gói tin. Giá trị sentinel (0xFFFF cho unsigned, 0x7FFF cho signed) đánh dấu cảm biến bị lỗi.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/packet_lora.png}
    \caption{Cấu trúc gói tin LoRa}
    \label{fig:packet_lora}
\end{figure}

Cơ chế provisioning cho phép cấu hình Node ID qua giao diện web. Khi chưa có cấu hình (hoặc sau factory reset), Sensor Node khởi động WiFi AP với SSID ``AirQuality-SN-Setup'', phục vụ trang web captive portal tại địa chỉ 192.168.4.1. Người dùng nhập Node ID, firmware lưu vào bộ nhớ NVS (Non-Volatile Storage) và khởi động lại ở chế độ hoạt động bình thường.

\textbf{Firmware Gateway} sử dụng kiến trúc superloop (không dùng FreeRTOS) do chỉ thực hiện hai tác vụ tuần tự: nhận gói tin \gls{LoRa} từ \gls{UART} và tích luỹ vào bộ đệm vòng (ring buffer). Khi đủ số lượng gói hoặc hết chu kỳ thời gian, Gateway serialize bộ đệm thành \gls{JSON} array và gửi lên tầng máy chủ qua \gls{HTTP} POST. Gateway cũng hỗ trợ cơ chế self-provisioning: tự đăng ký với Backend Server khi kết nối lần đầu.

\subsection{Thiết kế giao diện}
\label{subsection:4.2.3}

Giao diện hệ thống được thiết kế cho trình duyệt web, hỗ trợ responsive trên nhiều kích thước màn hình từ máy tính (độ phân giải tối thiểu 1280$\times$720) đến thiết bị di động (tối thiểu 375$\times$667). Hệ thống hỗ trợ hai ngôn ngữ (Tiếng Việt và Tiếng Anh) thông qua thư viện \texttt{react-i18next}.

Giao diện được tổ chức thành hai nhóm layout riêng biệt. \textbf{UserLayout} dành cho người dùng, gồm thanh điều hướng ngang (navbar) ở trên cùng với các liên kết đến bốn trang: Dashboard, Lịch sử, Bản đồ và Xếp hạng. \textbf{AdminLayout} dành cho quản trị viên, gồm thanh điều hướng dọc (sidebar) bên trái có thể thu gọn, chứa các mục: Dashboard, Gateways, Sensor Nodes, Cảnh báo, Cấu hình, Người dùng, Xuất dữ liệu và Nhật ký.

Hệ thống sử dụng bộ thành phần giao diện tái sử dụng (component library) tự xây dựng, bao gồm 28 component: Button, Modal, Badge, Input, Select, Table, Pagination, Card, Tooltip và các component chuyên biệt như AQICard, MetricCard, HealthAdvisory. Tất cả component tuân thủ thiết kế nhất quán: dark theme với bảng màu chính là xanh dương đậm, các trạng thái tương tác (hover, focus, disabled) được định nghĩa thống nhất, và hiệu ứng chuyển đổi mượt mà (transition 200ms).


\subsection{Thiết kế module Backend}
\label{subsection:4.2.4}

Phần này trình bày thiết kế chi tiết cho bốn module quan trọng nhất trên Backend Server.

\textbf{Module telemetryService} chịu trách nhiệm xử lý dữ liệu đo lường từ Gateway. Module này cung cấp hai hàm chính: \texttt{processTelemetry} (với tham số \texttt{gateway\_id}, \texttt{data}) xử lý gói dữ liệu batch và \texttt{processHeartbeat} (với tham số \texttt{gateway\_id}) xử lý tín hiệu heartbeat. Hàm \texttt{processTelemetry} thực hiện tuần tự ba bước: lọc trùng lặp qua module \texttt{dedupCache}, thực thi transaction cập nhật trạng thái thiết bị và lưu dữ liệu đo lường, sau đó kiểm tra ngưỡng cảnh báo ngoài transaction.

\textbf{Module aqiService} triển khai thuật toán tính \gls{AQI} theo chuẩn \gls{USEPA} đã trình bày ở Chương 3. Module cung cấp năm hàm: \texttt{calculateSubAQI} tính AQI thành phần cho một chất ô nhiễm, \texttt{calculateAQI} tính AQI tổng hợp (giá trị lớn nhất giữa PM2.5 và PM10), \texttt{getAQIInfo} ánh xạ giá trị AQI sang mức và mã màu, \texttt{getCO2Info} và \texttt{getTVOCInfo} đánh giá mức CO\textsubscript{2} và TVOC. Bảng breakpoint được lưu trữ dưới dạng mảng hằng số trong mã nguồn, tương ứng chính xác với Bảng \ref{table:bp_pm25} và Bảng \ref{table:bp_pm10} ở Chương 3.

\textbf{Module alertService} quản lý toàn bộ vòng đời cảnh báo. Module cung cấp sáu hàm: \texttt{getAlerts} truy vấn danh sách cảnh báo với hỗ trợ lọc và phân trang, \texttt{acknowledgeAlert} xác nhận xử lý, \texttt{deleteAlert} xóa cảnh báo, \texttt{checkThresholdsAndCreateAlerts} kiểm tra năm thông số (PM2.5, PM10, CO\textsubscript{2}, TVOC, nhiệt độ) so với ngưỡng cấu hình và tạo cảnh báo khi vượt ngưỡng, \texttt{createConnectivityAlerts} tạo cảnh báo mất kết nối cho thiết bị offline, và \texttt{cleanupOldAlerts} xóa cảnh báo quá 30 ngày. Cơ chế cooldown 15 phút ngăn chặn tạo cảnh báo trùng lặp cho cùng thiết bị và cùng thông số trong khoảng thời gian ngắn.

\textbf{Module stationService} cung cấp dữ liệu cho các trang người dùng. Hàm \texttt{getNearestStation} sử dụng PostGIS để truy vấn trạm đo gần nhất dựa trên toạ độ GPS của người dùng thông qua hàm \texttt{ST\_Distance}. Hàm \texttt{getStationHistory} truy vấn dữ liệu lịch sử từ Continuous Aggregate \texttt{hourly\_measurements} cho chế độ xem theo giờ, hoặc từ bảng \texttt{measurements} gốc cho chế độ xem theo ngày.

Để minh hoạ luồng tương tác giữa các thành phần, phần tiếp theo trình bày ba biểu đồ trình tự (sequence diagram) cho ba use case quan trọng nhất của hệ thống.

\textbf{Biểu đồ trình tự 1: Xử lý dữ liệu Telemetry.} Hình~\ref{fig:seq_telemetry} mô tả luồng xử lý khi Gateway gửi dữ liệu đo lường lên Backend — đây là luồng cốt lõi của hệ thống IoT. Gateway gửi HTTP POST chứa batch dữ liệu đến \texttt{telemetryController}. Controller chuyển tiếp cho \texttt{telemetryService}, module này thực hiện ba bước tuần tự: (i) lọc gói tin trùng lặp qua \texttt{dedupCache} (xử lý trường hợp nhiều Gateway cùng nhận một gói LoRa), (ii) thực thi database transaction để cập nhật trạng thái Gateway, cập nhật trạng thái Sensor Node và lưu dữ liệu đo lường vào bảng \texttt{measurements}, (iii) kiểm tra ngưỡng cảnh báo qua \texttt{alertService} — bước này chạy ngoài transaction để lỗi cảnh báo không ảnh hưởng đến luồng lưu dữ liệu chính. Sau đó, Controller tính chỉ số AQI qua \texttt{aqiService} và phát sự kiện \texttt{new\_telemetry\_data} qua Socket.IO đến Frontend. Nếu có cảnh báo mới, sự kiện \texttt{new-alert} cũng được phát để thông báo cho quản trị viên.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/seq_telemetry_ingestion.png}
    \caption{Biểu đồ trình tự xử lý dữ liệu Telemetry}
    \label{fig:seq_telemetry}
\end{figure}

\textbf{Biểu đồ trình tự 2: Xem Dashboard thời gian thực.} Hình~\ref{fig:seq_dashboard} mô tả luồng end-to-end từ khi người dùng truy cập trang Dashboard đến khi nhận cập nhật thời gian thực. Luồng gồm hai pha. Pha 1 (tải dữ liệu ban đầu): khi React mount component Dashboard, hook \texttt{useDashboard} gọi API \texttt{/api/stations/nearest} kèm toạ độ GPS của người dùng. Backend sử dụng PostGIS (\texttt{ST\_DistanceSphere}) để tìm trạm đo gần nhất, tính AQI và trả kết quả về Frontend để hiển thị. Pha 2 (cập nhật thời gian thực): hook \texttt{useSocket} kết nối WebSocket đến Backend. Mỗi khi có dữ liệu telemetry mới từ Gateway, Backend phát sự kiện \texttt{new\_telemetry\_data}; hook \texttt{useSocket} nhận sự kiện, cập nhật Zustand store, kích hoạt React Virtual DOM so sánh và chỉ vẽ lại các thành phần thay đổi trên giao diện.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/seq_dashboard_realtime.png}
    \caption{Biểu đồ trình tự xem Dashboard thời gian thực}
    \label{fig:seq_dashboard}
\end{figure}

\textbf{Biểu đồ trình tự 3: Xử lý cảnh báo vượt ngưỡng.} Hình~\ref{fig:seq_alert} mô tả chi tiết luồng kiểm tra và tạo cảnh báo khi dữ liệu đo lường vượt ngưỡng cấu hình. Sau khi \texttt{telemetryService} lưu dữ liệu thành công, module gọi hàm \texttt{checkThresholdsAndCreateAlerts} của \texttt{alertService}. Module \texttt{alertService} thực hiện bốn bước: (i) truy vấn bảng \texttt{alert\_configs} để lấy ngưỡng warn/danger cho năm thông số (PM2.5, PM10, CO\textsubscript{2}, TVOC, nhiệt độ), (ii) so sánh từng giá trị đo với ngưỡng tương ứng, (iii) kiểm tra cơ chế cooldown 15 phút — nếu cùng Sensor Node và cùng thông số đã có cảnh báo trong 15 phút gần đây thì bỏ qua để tránh tạo cảnh báo trùng lặp, (iv) tạo bản ghi cảnh báo mới trong bảng \texttt{alerts}. Kết quả trả về Controller, nếu có cảnh báo mới, Controller phát sự kiện \texttt{new-alert} qua Socket.IO để Admin Dashboard hiển thị thông báo tức thì.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/seq_alert_threshold.png}
    \caption{Biểu đồ trình tự xử lý cảnh báo vượt ngưỡng}
    \label{fig:seq_alert}
\end{figure}

\subsection{Thiết kế cơ sở dữ liệu}
\label{subsection:4.2.5}

Cơ sở dữ liệu được thiết kế trên PostgreSQL 15 với hai extension: TimescaleDB cho dữ liệu chuỗi thời gian và PostGIS cho dữ liệu không gian. Lược đồ gồm bảy bảng chính và một materialized view (trên biểu đồ Hình~\ref{fig:er_diagram} chỉ thể hiện 7 bảng chính).

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/er_database.png}
    \caption{Biểu đồ thực thể–quan hệ (ERD)}
    \label{fig:er_diagram}
\end{figure}

\textbf{Bảng users} lưu thông tin tài khoản với khóa chính là UUID tự sinh, bao gồm username (duy nhất), password\_hash (mã hóa bcrypt) và role (admin hoặc user).

\textbf{Bảng gateways} lưu thông tin Gateway với khóa chính là mã định danh dạng chuỗi (ví dụ ``GW\_001''), bao gồm tên, mô tả vị trí, trạng thái online/offline và thời điểm gửi dữ liệu cuối (last\_seen).

\textbf{Bảng sensor\_nodes} lưu thông tin Sensor Node với khóa ngoại tham chiếu đến bảng gateways (quan hệ 1-N: một Gateway quản lý nhiều Sensor Node, xóa Gateway sẽ xóa cascade các Sensor Node). Cột \texttt{geom} kiểu \texttt{geometry(Point, 4326)} lưu toạ độ GPS theo hệ tọa độ WGS84, cho phép truy vấn không gian qua PostGIS. Các cột \texttt{battery\_level} và \texttt{lora\_rssi} lưu thông tin trạng thái phần cứng.

\textbf{Bảng measurements} là hypertable TimescaleDB, được tự động phân mảnh theo cột thời gian \texttt{time}. Khóa chính composite gồm (\texttt{time}, \texttt{node\_id}) đảm bảo mỗi Sensor Node chỉ có một bản ghi tại mỗi thời điểm. Bảng lưu sáu thông số: PM2.5, PM10, CO\textsubscript{2}, TVOC, nhiệt độ và độ ẩm. Retention policy tự động xóa dữ liệu thô quá 3 tháng.

\textbf{Materialized view hourly\_measurements} là Continuous Aggregate tự động tính giá trị trung bình theo giờ cho mỗi Sensor Node. View này được TimescaleDB tự động cập nhật khi có dữ liệu mới ghi vào bảng \texttt{measurements}.

\textbf{Bảng alert\_configs} lưu cấu hình ngưỡng cảnh báo dưới dạng singleton row (chỉ một bản ghi với id = ``default''). Cấu hình bao gồm ngưỡng warn và danger cho bốn thông số (PM2.5, PM10, CO\textsubscript{2}, TVOC), ngưỡng nhiệt độ tối thiểu/tối đa, và chu kỳ lấy mẫu.

\textbf{Bảng alerts} lưu cảnh báo tự động với hai loại: ``threshold'' (vượt ngưỡng) và ``connectivity'' (mất kết nối). Mỗi cảnh báo ghi nhận thông số vi phạm (metric), giá trị đo được (value), ngưỡng so sánh (threshold) và trạng thái xác nhận (acknowledged). Ba chỉ mục trên cột \texttt{node\_id}, \texttt{created\_at} và \texttt{acknowledged} tối ưu hiệu năng truy vấn lọc và phân trang.

\textbf{Bảng audit\_logs} ghi lại mọi hành động quản trị, bao gồm người thực hiện (user\_id, username), loại thao tác (action: CREATE, UPDATE, DELETE), đối tượng thao tác (resource, resource\_id), chi tiết thay đổi (details dạng JSON) và địa chỉ IP. Ba chỉ mục trên cột \texttt{user\_id}, \texttt{created\_at} và \texttt{resource} phục vụ truy vấn tra cứu nhật ký.

\section{Xây dựng ứng dụng}
\label{section:4.3}

\subsection{Thư viện và công cụ sử dụng}
\label{subsection:4.3.1}

Bảng \ref{table:tools_backend} và Bảng \ref{table:tools_frontend} liệt kê các công cụ, thư viện và phiên bản sử dụng để phát triển hệ thống, phân nhóm theo Backend và Frontend.

\begin{table}[H]
\centering
\caption{Danh sách thư viện và công cụ phía Backend}
\label{table:tools_backend}
\small
\begin{tabularx}{\textwidth}{|X|X|c|}
\hline
\textbf{Mục đích} & \textbf{Công cụ} & \textbf{Phiên bản} \\
\hline
Runtime & Node.js & 22.x \\
\hline
Web framework & Express & 4.21.0 \\
\hline
ORM & Prisma Client & 7.5.0 \\
\hline
Giao tiếp real-time & Socket.IO & 4.7.5 \\
\hline
Xác thực JWT & jsonwebtoken & 9.0.3 \\
\hline
Mã hóa mật khẩu & bcryptjs & 3.0.3 \\
\hline
Bảo mật HTTP headers & Helmet & 8.1.0 \\
\hline
Tác vụ định kỳ & node-cron & 4.2.1 \\
\hline
Xuất CSV & json2csv & 6.0.0 \\
\hline
CSDL & PostgreSQL & 15 \\
\hline
Extension time-series & TimescaleDB & 2.11.0 \\
\hline
Extension geospatial & PostGIS & 3.3.2 \\
\hline
\end{tabularx}
\end{table}

\begin{table}[H]
\centering
\caption{Danh sách thư viện và công cụ phía Frontend}
\label{table:tools_frontend}
\small
\begin{tabularx}{\textwidth}{|X|X|c|}
\hline
\textbf{Mục đích} & \textbf{Công cụ} & \textbf{Phiên bản} \\
\hline
UI framework & React & 19.2.4 \\
\hline
Build tool & Vite & 8.0.4 \\
\hline
Routing & React Router & 7.14.0 \\
\hline
HTTP client & Axios & 1.14.0 \\
\hline
Bản đồ tương tác & Leaflet + React-Leaflet & 1.9.4 / 5.0.0 \\
\hline
Biểu đồ & ECharts & 6.0.0 \\
\hline
Đa ngôn ngữ & react-i18next & 17.0.2 \\
\hline
Quản lý trạng thái & Zustand & 5.0.12 \\
\hline
Icon & Lucide React & 1.7.0 \\
\hline
Socket client & socket.io-client & 4.8.3 \\
\hline
\end{tabularx}
\end{table}

Ngoài ra, hệ thống sử dụng các công cụ phát triển và triển khai: VS Code làm IDE chính, PlatformIO với framework Arduino để phát triển firmware ESP32, Docker và Docker Compose để container hóa, Nginx làm reverse proxy, và Certbot để quản lý chứng chỉ SSL.

\subsection{Kết quả đạt được}
\label{subsection:4.3.2}

Sản phẩm cuối cùng bao gồm bốn thành phần được đóng gói riêng biệt. Thứ nhất, \textbf{Firmware Sensor Node} chạy trên ESP32, sử dụng cơ chế Deep-sleep để tối ưu năng lượng thông qua việc thức giấc định kỳ để tuần tự đọc các cảm biến và gửi qua LoRa. Thứ hai, \textbf{Firmware Gateway} chạy trên ESP32, sử dụng kiến trúc superloop để nhận gói LoRa và chuyển tiếp lên Backend Server qua WiFi. Thứ ba, \textbf{Backend Server} là ứng dụng Node.js/Express cung cấp \gls{REST} \gls{API} và giao tiếp real-time qua Socket.IO. Thứ tư, \textbf{Frontend} là ứng dụng React \gls{SPA} được build bởi Vite thành các tệp tĩnh, phục vụ bởi Nginx.

Bảng \ref{table:stats} thống kê quy mô mã nguồn của từng thành phần.

\begin{table}[H]
\centering
\caption{Thống kê quy mô mã nguồn hệ thống}
\label{table:stats}
\small
\begin{tabularx}{\textwidth}{|X|X|c|X|}
\hline
\textbf{Thành phần} & \textbf{Ngôn ngữ chính} & \textbf{LOC} & \textbf{Ghi chú} \\
\hline
Firmware Sensor Node & C/C++ & 1262 & Deep-sleep \\
\hline
Firmware Gateway & C/C++ & 1608 & Superloop \\
\hline
Backend Server & JavaScript (Node.js) & 2732 & Express 4.21 \\
\hline
Frontend & JavaScript (React) & 8050 & Vite 8.0 \\
\hline
Database & SQL & 107 & PostgreSQL \\
\hline
Triển khai & YAML, Nginx conf & 277 & Docker Compose \\
\hline  
\end{tabularx}
\end{table}

Bảng \ref{table:req_mapping} đối sánh kết quả đạt được với các yêu cầu chức năng và phi chức năng đã đặc tả ở Chương 2, cho thấy hệ thống đã hiện thực đầy đủ toàn bộ chín chức năng và đáp ứng năm yêu cầu phi chức năng.

\begin{table}[H]
\centering
\caption{Đối sánh yêu cầu đặc tả với kết quả đạt được}
\label{table:req_mapping}
\small
\begin{tabularx}{\textwidth}{|c|X|c|X|}
\hline
\textbf{STT} & \textbf{Yêu cầu (Chương 2)} & \textbf{Trạng thái} & \textbf{Minh chứng} \\
\hline
\multicolumn{4}{|l|}{\textit{Yêu cầu chức năng}} \\
\hline
1 & Xem dashboard (Bảng~\ref{table:uc_dashboard}) & Đạt & Mục~\ref{subsection:4.3.3}, kiểm thử Bảng~\ref{table:test_dashboard} \\
\hline
2 & Xem lịch sử dữ liệu (Bảng~\ref{table:uc_history}) & Đạt & Mục~\ref{subsection:4.3.3} \\
\hline
3 & Xem bản đồ AQI & Đạt & Mục~\ref{subsection:4.3.3} \\
\hline
4 & Xem xếp hạng ô nhiễm & Đạt & Mục~\ref{subsection:4.3.3} \\
\hline
5 & Quản lý thiết bị (Bảng~\ref{table:uc_device}) & Đạt & Mục~\ref{subsection:4.3.3}, kiểm thử Bảng~\ref{table:test_device} \\
\hline
6 & Giám sát cảnh báo (Bảng~\ref{table:uc_alert}) & Đạt & Mục~\ref{subsection:4.3.3}, kiểm thử Bảng~\ref{table:test_alert} \\
\hline
7 & Cấu hình hệ thống & Đạt & Mục~\ref{subsection:4.3.3} \\
\hline
8 & Quản lý tài khoản & Đạt & Mục~\ref{subsection:4.3.3} \\
\hline
9 & Xuất dữ liệu CSV (Bảng~\ref{table:uc_export}) & Đạt & Mục~\ref{subsection:4.3.3} \\
\hline
\multicolumn{4}{|l|}{\textit{Yêu cầu phi chức năng (Bảng~\ref{table:nfr})}} \\
\hline
10 & Độ tin cậy: 24/7, phát hiện offline & Đạt & Cron job 5 phút, ring buffer trên Gateway \\
\hline
11 & Khả năng mở rộng: Hub-Spoke & Đạt & Mục~\ref{subsection:4.1.1}, tự đăng ký Gateway \\
\hline
12 & Tính dễ sử dụng: responsive, đa ngôn ngữ & Đạt & Mục~\ref{subsection:4.2.3}, i18n Việt--Anh \\
\hline
13 & Bảo mật: JWT, bcrypt, audit log & Đạt & Mục~\ref{subsection:4.2.4}, kiểm thử TC6 Bảng~\ref{table:test_device} \\
\hline
\end{tabularx}
\end{table}

\subsection{Minh họa các chức năng chính}
\label{subsection:4.3.3}

Phần này minh hoạ các chức năng chính của hệ thống thông qua giao diện thực tế trên môi trường production.

\textbf{Dashboard chất lượng không khí.} Khi người dùng truy cập trang Dashboard, hệ thống yêu cầu quyền truy cập vị trí, sau đó ưu tiên hiển thị dữ liệu của trạm đo gần nhất. Giao diện trang chủ được bố cục từ trên xuống dưới với hàng đầu tiên là sáu thẻ thông số nổi bật hiển thị lần lượt các chỉ số: \gls{AQI} tổng hợp, PM2.5, PM10, CO\textsubscript{2}, nhiệt độ và độ ẩm, mỗi thẻ được thiết kế kèm viền màu cảnh báo mức độ tương ứng. Ngay bên dưới là khu vực Bản đồ AQI khu vực cùng danh sách các trạm đo lân cận. Ở nửa dưới bảng điều khiển, hệ thống cung cấp biểu đồ lịch sử theo dõi xu hướng thay đổi của các thông số theo thời gian, kết hợp với các thẻ tính năng bổ trợ như Khuyến nghị sức khỏe và Thời tiết hiện tại. Dữ liệu được cập nhật liên tục theo thời gian thực khi Backend phát sự kiện qua Socket.IO.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/dashboard.png}
    \caption{Dashboard chất lượng không khí}
    \label{fig:dashboard}
\end{figure}

\textbf{Dashboard quản trị hệ thống.} Khi quản trị viên đăng nhập và truy cập trang Tổng quan (Admin Dashboard), hệ thống hiển thị bức tranh toàn cảnh về trạng thái hoạt động: số lượng Sensor Node và Gateway đang hoạt động, số cảnh báo chờ xử lý, chỉ số AQI trung bình toàn hệ thống và uptime. Phần bên trái liệt kê biểu đồ AQI theo trạm theo thời gian thực; phần bên phải tổng hợp các cảnh báo gần đây và nhật ký hoạt động (audit log) mới nhất, giúp quản trị viên nắm bắt nhanh tình hình mà không cần điều hướng qua nhiều trang.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/dashboard_quan_tri.png}
    \caption{Dashboard tổng quan quản trị hệ thống}
    \label{fig:dashboard_quan_tri}
\end{figure}

\textbf{Lịch sử dữ liệu.} Trang lịch sử cho phép người dùng chọn trạm đo, thông số cần xem và chế độ hiển thị (theo giờ hoặc theo ngày). Biểu đồ được vẽ bằng ECharts, hỗ trợ tooltip và responsive. Chế độ theo giờ truy vấn từ Continuous Aggregate \texttt{hourly\_measurements} để đảm bảo hiệu năng.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/lich_su.png}
    \caption{Giao diện trang Lịch sử dữ liệu}
    \label{fig:lich_su}
\end{figure}

\textbf{Bản đồ AQI.} Trang bản đồ hiển thị toàn bộ các trạm đo trên bản đồ Leaflet với tile OpenStreetMap. Mỗi trạm được đánh dấu bằng marker hình tròn với mã màu AQI. Người dùng nhấn vào marker để xem popup thông tin chi tiết gồm tên trạm, giá trị AQI, PM2.5 và thời gian cập nhật cuối.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/ban_do_aqi.png}
    \caption{Giao diện trang Bản đồ AQI}
    \label{fig:ban_do_aqi}
\end{figure}

\textbf{Các chức năng quản trị khác.} Hình~\ref{fig:xep_hang} đến Hình~\ref{fig:quan_ly_thiet_bi} minh họa các chức năng hỗ trợ bao gồm: xếp hạng ô nhiễm (sắp xếp trạm theo AQI), quản lý tài khoản (phân quyền admin/user), xuất dữ liệu (tải CSV), nhật ký hệ thống (audit log), giám sát cảnh báo, cấu hình ngưỡng, và quản lý thiết bị.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/xep_hang.png}
    \caption{Giao diện trang Xếp hạng ô nhiễm}
    \label{fig:xep_hang}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/xuat_csv.png}
    \caption{Giao diện chức năng Xuất dữ liệu CSV}
    \label{fig:xuat_csv}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/quan_ly_tai_khoan.png}
    \caption{Giao diện trang Quản lý tài khoản}
    \label{fig:quan_ly_tai_khoan}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/nhat_ky_he_thong.png}
    \caption{Nhật ký hệ thống (audit log)}
    \label{fig:nhat_ky_he_thong}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/giam_sat_canh_bao.png}
    \caption{Giao diện giám sát cảnh báo}
    \label{fig:giam_sat_canh_bao}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/cau_hinh.png}
    \caption{Giao diện cấu hình ngưỡng cảnh báo}
    \label{fig:cau_hinh}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/quan_ly_thiet_bi.png}
    \caption{Giao diện quản lý thiết bị}
    \label{fig:quan_ly_thiet_bi}
\end{figure}

\section{Kiểm thử}
\label{section:4.4}

Hệ thống được kiểm thử bằng hai phương pháp. Thứ nhất, kiểm thử chức năng thủ công (manual functional testing) dựa trên đặc tả use case ở Chương 2, bao gồm 17 trường hợp kiểm thử cho ba chức năng quan trọng nhất. Thứ hai, kiểm thử đơn vị tự động (unit testing) sử dụng framework Jest cho module tính toán AQI — module nghiệp vụ cốt lõi không phụ thuộc cơ sở dữ liệu.

\subsection{Kiểm thử chức năng Xem dashboard}
\label{subsection:4.4.1}

Bảng \ref{table:test_dashboard} trình bày các trường hợp kiểm thử cho chức năng xem dashboard chất lượng không khí.

\begin{table}[H]
\centering
\caption{Trường hợp kiểm thử chức năng Xem dashboard}
\label{table:test_dashboard}
\small
\begin{tabularx}{\textwidth}{|c|X|X|X|c|}
\hline
\textbf{STT} & \textbf{Mô tả} & \textbf{Kết quả mong đợi} & \textbf{Kết quả thực tế} & \textbf{Đạt} \\
\hline
1 & Truy cập dashboard, cho phép truy cập vị trí & Hiển thị dữ liệu trạm gần nhất & Đúng & Đạt \\
\hline
2 & Truy cập dashboard, từ chối truy cập vị trí & Hiển thị dữ liệu trạm mặc định & Đúng & Đạt \\
\hline
3 & Dashboard nhận dữ liệu mới qua Socket.IO & Các thẻ metric cập nhật tự động & Đúng & Đạt \\
\hline
4 & Không có trạm nào hoạt động & Hiển thị thông báo ``Không có dữ liệu'' & Đúng & Đạt \\
\hline
5 & Kiểm tra mã màu AQI & Thẻ AQI và badge hiển thị đúng mã màu theo mức AQI & Đúng & Đạt \\
\hline
\end{tabularx}
\end{table}

\subsection{Kiểm thử chức năng Quản lý thiết bị}
\label{subsection:4.4.2}

Bảng \ref{table:test_device} trình bày các trường hợp kiểm thử cho chức năng quản lý thiết bị.

\begin{table}[H]
\centering
\caption{Trường hợp kiểm thử chức năng Quản lý thiết bị}
\label{table:test_device}
\small
\begin{tabularx}{\textwidth}{|c|X|X|X|c|}
\hline
\textbf{STT} & \textbf{Mô tả} & \textbf{Kết quả mong đợi} & \textbf{Kết quả thực tế} & \textbf{Đạt} \\
\hline
1 & Thêm mới Gateway với thông tin hợp lệ & Gateway được tạo, hiển thị trong danh sách & Đúng & Đạt \\
\hline
2 & Thêm mới Sensor Node, chọn Gateway cha & Sensor Node được tạo, liên kết đúng Gateway & Đúng & Đạt \\
\hline
3 & Sửa thông tin thiết bị & Thông tin cập nhật thành công & Đúng & Đạt \\
\hline
4 & Xóa Gateway có Sensor Node con & Xóa cascade cả Sensor Node con & Đúng & Đạt \\
\hline
5 & Thêm thiết bị với dữ liệu không hợp lệ (để trống tên) & Hiển thị thông báo lỗi & Đúng & Đạt \\
\hline
6 & Kiểm tra audit log sau thao tác & Nhật ký ghi nhận đúng thao tác, người thực hiện. & Đúng & Đạt \\
\hline
\end{tabularx}
\end{table}

\subsection{Kiểm thử chức năng Giám sát cảnh báo}
\label{subsection:4.4.3}

Bảng \ref{table:test_alert} trình bày các trường hợp kiểm thử cho chức năng giám sát và xử lý cảnh báo.

\begin{table}[H]
\centering
\caption{Trường hợp kiểm thử chức năng Giám sát cảnh báo}
\label{table:test_alert}
\small
\begin{tabularx}{\textwidth}{|c|X|X|X|c|}
\hline
\textbf{STT} & \textbf{Mô tả} & \textbf{Kết quả mong đợi} & \textbf{Kết quả thực tế} & \textbf{Đạt} \\
\hline
1 & Gửi dữ liệu PM2.5 vượt ngưỡng warn & Cảnh báo mức warn được tạo tự động & Đúng & Đạt \\
\hline
2 & Gửi dữ liệu PM2.5 vượt ngưỡng danger & Cảnh báo mức danger được tạo, phát qua Socket.IO & Đúng & Đạt \\
\hline
3 & Gửi dữ liệu vượt ngưỡng lần 2 trong 15 phút & Không tạo cảnh báo trùng (cooldown) & Đúng & Đạt \\
\hline
4 & Thiết bị không gửi dữ liệu quá thời gian quy định & Cron job tạo cảnh báo mất kết nối & Đúng & Đạt \\
\hline
5 & Lọc cảnh báo theo mức độ & Danh sách chỉ hiển thị cảnh báo đúng mức độ & Đúng & Đạt \\
\hline
6 & Xác nhận cảnh báo (acknowledge) & Trạng thái cập nhật thành ``đã xác nhận'' & Đúng & Đạt \\
\hline
\end{tabularx}
\end{table}

\subsection{Kiểm thử chức năng Xem lịch sử dữ liệu}
\label{subsection:4.4.4}

Bảng \ref{table:test_history} trình bày các trường hợp kiểm thử cho chức năng xem lịch sử dữ liệu.

\begin{table}[H]
\centering
\caption{Trường hợp kiểm thử chức năng Xem lịch sử dữ liệu}
\label{table:test_history}
\small
\begin{tabularx}{\textwidth}{|c|X|X|X|c|}
\hline
\textbf{STT} & \textbf{Mô tả} & \textbf{Kết quả mong đợi} & \textbf{Kết quả thực tế} & \textbf{Đạt} \\
\hline
1 & Chọn trạm đo và xem biểu đồ theo ngày (30 ngày) & Biểu đồ hiển thị đúng dữ liệu & Phản hồi dưới 3000ms, đúng dữ liệu & Đạt \\
\hline
2 & Chuyển chế độ hiển thị từ theo ngày sang theo giờ & Biểu đồ cập nhật đúng dạng dữ liệu & Đúng & Đạt \\
\hline
3 & Tương tác với biểu đồ ECharts & Tooltip hiển thị chi tiết & Đúng & Đạt \\
\hline
\end{tabularx}
\end{table}

\subsection{Kiểm thử chức năng Xem bản đồ AQI}
\label{subsection:4.4.5}

Bảng \ref{table:test_map} trình bày các trường hợp kiểm thử cho chức năng xem bản đồ AQI.

\begin{table}[H]
\centering
\caption{Trường hợp kiểm thử chức năng Xem bản đồ AQI}
\label{table:test_map}
\small
\begin{tabularx}{\textwidth}{|c|X|X|X|c|}
\hline
\textbf{STT} & \textbf{Mô tả} & \textbf{Kết quả mong đợi} & \textbf{Kết quả thực tế} & \textbf{Đạt} \\
\hline
1 & Truy cập trang bản đồ & Hiển thị bản đồ Leaflet với tất cả marker trạm đo & Hiển thị đủ marker, mã màu đúng & Đạt \\
\hline
2 & Nhấn vào marker trạm đo & Popup hiển thị tên trạm, AQI, PM2.5, thời gian & Hiển thị đầy đủ thông tin & Đạt \\
\hline
3 & Kiểm tra mã màu marker theo mức AQI & Màu marker tương ứng với mức AQI (xanh, vàng, cam, đỏ) & Màu đúng theo chuẩn USEPA & Đạt \\
\hline
\end{tabularx}
\end{table}

\subsection{Kiểm thử chức năng Xuất dữ liệu CSV}
\label{subsection:4.4.6}

Bảng \ref{table:test_export} trình bày các trường hợp kiểm thử cho chức năng xuất dữ liệu CSV.

\begin{table}[H]
\centering
\caption{Trường hợp kiểm thử chức năng Xuất dữ liệu CSV}
\label{table:test_export}
\small
\begin{tabularx}{\textwidth}{|c|X|X|X|c|}
\hline
\textbf{STT} & \textbf{Mô tả} & \textbf{Kết quả mong đợi} & \textbf{Kết quả thực tế} & \textbf{Đạt} \\
\hline
1 & Xuất CSV với khoảng thời gian hợp lệ & Tải file CSV chứa đầy đủ cột dữ liệu & File CSV tải thành công, đủ cột & Đạt \\
\hline
2 & Xuất CSV khi không có dữ liệu trong khoảng thời gian & Thông báo ``Không có dữ liệu'' & Hiển thị thông báo đúng & Đạt \\
\hline
\end{tabularx}
\end{table}

\subsection{Kiểm thử đơn vị module tính AQI}
\label{subsection:4.4.7}

Module \texttt{aqiService} được kiểm thử đơn vị bằng framework Jest. Module này chứa logic tính toán AQI theo chuẩn \gls{USEPA}, là module nghiệp vụ thuần (pure function) không phụ thuộc cơ sở dữ liệu hay dịch vụ bên ngoài, do đó phù hợp để viết unit test trực tiếp mà không cần mock.

Bảng \ref{table:test_aqi_unit} trình bày kết quả kiểm thử đơn vị cho năm hàm trong module.

\begin{table}[H]
\centering
\caption{Kết quả kiểm thử đơn vị module \texttt{aqiService}}
\label{table:test_aqi_unit}
\small
\begin{tabularx}{\textwidth}{|c|X|X|X|c|}
\hline
\textbf{STT} & \textbf{Hàm kiểm thử} & \textbf{Mô tả} & \textbf{Kết quả mong đợi} & \textbf{Đạt} \\
\hline
1 & \texttt{calculateSubAQI} & PM2.5 = 0 (biên dưới) & AQI = 0 & Đạt \\
\hline
2 & \texttt{calculateSubAQI} & PM2.5 = 12.0 (biên trên Good) & AQI = 50 & Đạt \\
\hline
3 & \texttt{calculateSubAQI} & PM2.5 = 35.4 (biên trên Moderate) & AQI = 100 & Đạt \\
\hline
4 & \texttt{calculateSubAQI} & PM2.5 = 55.5 (biên dưới Unhealthy) & AQI = 151 & Đạt \\
\hline
5 & \texttt{calculateSubAQI} & PM2.5 = 500.4 (biên trên max) & AQI = 500 & Đạt \\
\hline
6 & \texttt{calculateSubAQI} & PM2.5 = 600 (vượt ngưỡng) & AQI = 500 & Đạt \\
\hline
7 & \texttt{calculateAQI} & PM2.5 = 25, PM10 = 50 & Max(78, 46) = 78 & Đạt \\
\hline
8 & \texttt{calculateAQI} & PM2.5 = 0, PM10 = 0 & AQI = 0 & Đạt \\
\hline
9 & \texttt{calculateAQI} & PM2.5 = 5, PM10 = 200 & AQI theo PM10 $>$ 100 & Đạt \\
\hline
10 & \texttt{getAQIInfo} & AQI = 25 & Mức Good & Đạt \\
\hline
11 & \texttt{getAQIInfo} & AQI = 75 & Mức Moderate & Đạt \\
\hline
12 & \texttt{getAQIInfo} & AQI = 125 & Mức Unhealthy Sensitive & Đạt \\
\hline
13 & \texttt{getAQIInfo} & AQI = 175 & Mức Unhealthy & Đạt \\
\hline
14 & \texttt{getAQIInfo} & AQI = 250 & Mức Very Unhealthy & Đạt \\
\hline
15 & \texttt{getAQIInfo} & AQI = 400 & Mức Hazardous & Đạt \\
\hline
16 & \texttt{getCO2Info} & CO\textsubscript{2} = 400 & Mức Tốt & Đạt \\
\hline
17 & \texttt{getCO2Info} & CO\textsubscript{2} = 1200 & Mức Kém & Đạt \\
\hline
18 & \texttt{getTVOCInfo} & TVOC = 30 & Mức Tốt & Đạt \\
\hline
19 & \texttt{getTVOCInfo} & TVOC = 500 & Mức Kém & Đạt \\
\hline
\end{tabularx}
\end{table}

Kết quả: 19/19 trường hợp kiểm thử đều đạt (pass). Các test case tập trung kiểm tra giá trị biên (boundary values) của bảng breakpoint AQI, đảm bảo công thức nội suy tuyến tính trả về đúng giá trị tại các điểm chuyển mức.

\subsection{Tổng kết kiểm thử}
\label{subsection:4.4.8}

Tổng cộng 44 trường hợp kiểm thử đã được thực hiện, chia thành hai nhóm. Nhóm kiểm thử chức năng thủ công gồm 25 test case trên sáu chức năng: Xem dashboard (5 test case), Quản lý thiết bị (6 test case), Giám sát cảnh báo (6 test case), Xem lịch sử dữ liệu (3 test case), Xem bản đồ AQI (3 test case), và Xuất dữ liệu CSV (2 test case). Nhóm kiểm thử đơn vị tự động gồm 19 test case cho module tính AQI. Tất cả 44/44 trường hợp đều đạt, tỉ lệ 100\%. Kết quả cho thấy cả logic nghiệp vụ cốt lõi (tính AQI) lẫn các chức năng end-to-end đều hoạt động đúng theo đặc tả. Các chức năng còn lại (Xếp hạng, Cấu hình, Quản lý tài khoản) có độ phức tạp thấp hơn và đã được kiểm tra qua quá trình phát triển.

\section{Triển khai}
\label{section:4.5}

Hệ thống được triển khai trên máy chủ ảo (VPS) DigitalOcean với cấu hình 1 vCPU, 1 GB RAM, 25 GB SSD, chạy hệ điều hành Ubuntu 22.04 LTS. Domain truy cập là \texttt{datn.thamnguyen.dev}, được cấu hình HTTPS thông qua chứng chỉ SSL miễn phí từ Let's Encrypt.

Hình~\ref{fig:deployment_diagram} mô tả biểu đồ triển khai UML của hệ thống, thể hiện các node vật lý, môi trường thực thi và đường truyền thông giữa chúng.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/deployment_diagram.png}
    \caption{Biểu đồ triển khai hệ thống}
    \label{fig:deployment_diagram}
\end{figure}

Mô hình triển khai sử dụng Docker Compose để quản lý bốn container. Container \textbf{db} chạy image \texttt{timescale/timescaledb-ha:pg15}, lưu trữ dữ liệu trên volume \texttt{pg\_data} và chỉ mở cổng 5432 trên localhost (truy cập qua SSH tunnel). Container \textbf{backend} chạy ứng dụng Node.js, kết nối đến container db thông qua mạng nội bộ Docker. Container \textbf{nginx} chạy Nginx phục vụ hai chức năng: (i) reverse proxy chuyển tiếp các yêu cầu \gls{HTTP} và WebSocket đến Backend, và (ii) phục vụ các tệp tĩnh Frontend đã được build bởi Vite. Container \textbf{certbot} tự động gia hạn chứng chỉ SSL mỗi 12 giờ.

Quy trình triển khai được thực hiện bằng một lệnh duy nhất: \texttt{docker compose --env-file .env.production -f docker-compose.prod.yml up --build -d}. Docker Compose tự động build image cho Backend và Nginx từ Dockerfile, khởi tạo cơ sở dữ liệu từ \texttt{init.sql}, và kết nối các container qua mạng nội bộ \texttt{app-network}. Toàn bộ cấu hình nhạy cảm (mật khẩu database, JWT secret) được lưu trong file \texttt{.env.production} không đưa lên kho mã nguồn.

Hình~\ref{fig:docker_ps} cho thấy bốn container đang chạy ổn định trên môi trường production.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/docker_ps.png}
    \caption{Kết quả lệnh \texttt{docker ps} trên máy chủ production}
    \label{fig:docker_ps}
\end{figure}

Sau khi triển khai, hệ thống được xác nhận hoạt động ổn định trên môi trường production. Giao diện web truy cập được tại địa chỉ \texttt{https://datn.thamnguyen.dev} với chứng chỉ SSL hợp lệ. Kết nối WebSocket (Socket.IO) hoạt động đúng: khi Gateway gửi dữ liệu mới, dashboard cập nhật trong thời gian thực mà không cần tải lại trang. Bốn container Docker (db, backend, nginx, certbot) duy trì trạng thái healthy liên tục.

\subsection{Kết quả vận hành thực tế}
\label{subsection:4.5.1}

Hệ thống được đưa vào vận hành thử nghiệm để đánh giá các chỉ tiêu phi chức năng và tính ổn định của thiết bị. Kết quả đánh giá tập trung vào hai khía cạnh cốt lõi: hiệu năng và độ tin cậy.

\textbf{Đánh giá hiệu năng:} Hệ thống cho thấy tốc độ phản hồi nhanh chóng và ổn định trong quá trình thử nghiệm. Khi người dùng truy cập trang chủ (Dashboard), dữ liệu của trạm đo gần nhất được hệ thống tải về gần như tức thời. Đối với truy vấn khối lượng dữ liệu lớn như dải lịch sử 30 ngày (yêu cầu NFR1 ở Bảng 2.5), nhờ ứng dụng cơ chế Continuous Aggregate của TimescaleDB, dữ liệu biểu đồ được hệ thống trích xuất mượt mà, luôn đáp ứng dễ dàng tiêu chuẩn phản hồi dưới 3 giây. Ở tính năng thời gian thực, luồng dữ liệu thông qua Socket.IO hoạt động trơn tru: ngay sau khi có dữ liệu mới từ Gateway, giao diện người dùng lập tức tự động cập nhật số liệu mới nhất mà không xảy ra độ trễ (lag) đáng kể nào.

\textbf{Đánh giá độ tin cậy:} Các container Docker duy trì uptime 100\% trong suốt thời gian theo dõi 7 ngày thử nghiệm (xác nhận qua lệnh \texttt{docker ps} và log hệ thống), không xảy ra hiện tượng tràn bộ nhớ hay khởi động lại ngoài ý muốn. Tác vụ định kỳ phát hiện thiết bị offline hoạt động chính xác: cảnh báo mất kết nối tự động được tạo và gửi đến frontend sau đúng 40 phút đối với Sensor Node (do đặc thù tiết kiệm pin), và sau 5 phút đối với Gateway khi hệ thống ngừng nhận được dữ liệu.

\textbf{Kịch bản thử nghiệm mất kết nối thiết bị:} Nhằm kiểm chứng quá trình giám sát kết nối theo thời gian thực (NFR10 ở Bảng 2.5), hệ thống được thực nghiệm với kịch bản mất kết nối chi tiết từng bước như sau:

\textbf{Bước 1:} Bật Gateway có ID: \texttt{GW\_013}. Gateway sẽ gửi heartbeat lên server:
\begin{figure}[H]
    \centering
    \includegraphics[angle=-90, origin=c, width=0.6\textwidth]{Hinhve/mat_ket_noi_01.jpg}
    \caption{Gateway GW\_013 gửi heartbeat}
    \label{fig:tkmkn_01}
\end{figure}

Server đã ghi nhận \texttt{GW\_013} online trên hệ thống:
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/mat_ket_noi_02.png}
    \caption{Server ghi nhận Gateway GW\_013 online}
    \label{fig:tkmkn_02}
\end{figure}

\textbf{Bước 2:} Tiếp tục bật Sensor Node có ID: \texttt{4C (NODE\_076)}:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Hinhve/mat_ket_noi_03.jpg}
    \caption{Bật Sensor Node 4C (NODE\_076)}
    \label{fig:tkmkn_03}
\end{figure}

Lúc này Gateway đã nhận được gói tin từ Sensor Node \texttt{4C (NODE\_076)} và gửi lên server:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Hinhve/mat_ket_noi_04.jpg}
    \caption{Gateway nhận gói tin từ Sensor Node 4C (NODE\_076)}
    \label{fig:tkmkn_04}
\end{figure}

Server nhận được gói tin từ Gateway và đánh dấu Sensor Node \texttt{4C (NODE\_076)} chuyển sang trạng thái online:
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/mat_ket_noi_05.png}
    \caption{Server nhận gói tin và đánh dấu Sensor Node 4C online}
    \label{fig:tkmkn_05}
\end{figure}

\textbf{Bước 3:} Tắt Sensor Node \texttt{4C (NODE\_076)}. Do đặc thù tối ưu năng lượng bằng chế độ deep-sleep, tham số timeout được cấu hình dài hơn. Lúc này Gateway vẫn sẽ hiện online vì vẫn gửi heartbeat đều đặn lên server (Hình~\ref{fig:tkmkn_08}). Sau 40 phút server không nhận được gói tin nào từ Sensor Node \texttt{4C}, hệ thống sẽ tự động chuyển trạng thái của Node này sang offline và cảnh báo tới admin:
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/mat_ket_noi_06.png}
    \caption{Hệ thống ghi nhận Sensor Node 4C (NODE\_076) offline}
    \label{fig:tkmkn_06}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/mat_ket_noi_07.png}
    \caption{Hệ thống cảnh báo Sensor Node 4C (NODE\_076) offline}
    \label{fig:tkmkn_07}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/mat_ket_noi_08.png}
    \caption{Gateway vẫn duy trì trạng thái online}
    \label{fig:tkmkn_08}
\end{figure}

\textbf{Bước 4:} Tiến hành tắt Gateway. Khác với Sensor Node, Gateway sử dụng nguồn điện trực tiếp và có chu kỳ phát hiện bất thường ngắn hơn. Sẽ có độ trễ cảnh báo nhỏ, sau đúng 5 phút không nhận được heartbeat, server lập tức đánh dấu Gateway là offline và cảnh báo tới admin:
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/mat_ket_noi_09.png}
    \caption{Hệ thống ghi nhận Gateway offline}
    \label{fig:tkmkn_09}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/mat_ket_noi_10.png}
    \caption{Hệ thống cảnh báo Gateway offline}
    \label{fig:tkmkn_10}
\end{figure}

\textbf{Kịch bản thử nghiệm môi trường ô nhiễm (đốt hương):} Để đánh giá tốc độ phản hồi và độ nhạy của các cảm biến, một thử nghiệm đốt hương (nhang) được thực hiện trong không gian phòng kín (diện tích $\sim$15m² -- xem Hình \ref{fig:thuc_nghiem_dot_huong_setup} về cách bố trí không gian). Quá trình thử nghiệm diễn ra trong 17 phút và được chia làm ba pha: pha Baseline (từ 16:20 đến 16:24, phòng đóng cửa, không có nguồn ô nhiễm), pha Đốt hương (từ 16:25 đến 16:32, thắp một nén hương cách Sensor Node khoảng 1 mét), và pha Phục hồi (từ 16:33 đến 16:37, dập tắt hương và mở cửa sổ để không khí lưu thông). Dữ liệu thu thập được từ Sensor Node trong suốt quá trình thử nghiệm được trình bày chi tiết tại Bảng \ref{table:thuc_nghiem_dot_huong}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{Hinhve/thuc_nghiem_dot_huong_setup.jpg}
    \caption{Bố trí thiết bị thử nghiệm đốt hương}
    \label{fig:thuc_nghiem_dot_huong_setup}
\end{figure}

\begin{table}[H]
\centering
\caption{Dữ liệu đo lường kịch bản đốt hương}
\label{table:thuc_nghiem_dot_huong}
\small
\begin{tabularx}{\textwidth}{|X|X|X|X|X|X|X|X|}
\hline
\textbf{Thời gian} & \textbf{Pha} & \textbf{PM2.5} & \textbf{PM10} & \textbf{eCO$_2$} & \textbf{eTVOC} & \textbf{Nhiệt độ} & \textbf{Độ ẩm} \\
\hline
16:20 & Baseline & 18 & 19 & 451 & 7 & 32.6 & 62.2 \\
\hline
16:21 & Baseline & 20 & 22 & 433 & 5 & 32.6 & 62.2 \\
\hline
16:22 & Baseline & 18 & 19 & 436 & 5 & 32.5 & 62.5 \\
\hline
16:23 & Baseline & 19 & 20 & 442 & 6 & 32.5 & 62.4 \\
\hline
16:24 & Baseline & 20 & 22 & 450 & 7 & 32.5 & 62.3 \\
\hline
\textbf{16:25} & \textbf{Ô nhiễm} & 22 & 24 & 448 & 7 & 32.6 & 62.3 \\
\hline
16:26 & Ô nhiễm & 416 & 513 & 543 & 21 & 32.6 & 62.4 \\
\hline
16:27 & Ô nhiễm & 321 & 374 & 646 & 37 & 32.6 & 62.5 \\
\hline
16:28 & Ô nhiễm & 199 & 231 & 1178 & 117 & 32.6 & 62.5 \\
\hline
16:29 & Ô nhiễm & 213 & 249 & 1801 & 451 & 32.6 & 62.5 \\
\hline
16:30 & Ô nhiễm & 332 & 394 & 1095 & 105 & 32.6 & 62.5 \\
\hline
16:31 & Ô nhiễm & 247 & 293 & 945 & 83 & 32.6 & 62.5 \\
\hline
16:32 & Ô nhiễm & 262 & 306 & 902 & 76 & 32.5 & 62.7 \\
\hline
\textbf{16:33} & \textbf{Phục hồi} & 237 & 275 & 683 & 43 & 32.5 & 62.3 \\
\hline
16:34 & Phục hồi & 149 & 170 & 529 & 19 & 32.3 & 62.0 \\
\hline
16:35 & Phục hồi & 71 & 82 & 481 & 12 & 32.4 & 61.3 \\
\hline
16:36 & Phục hồi & 48 & 60 & 418 & 2 & 32.4 & 61.2 \\
\hline
16:37 & Phục hồi & 41 & 50 & 405 & 0 & 32.4 & 61.2 \\
\hline
\end{tabularx}
\end{table}

\textbf{Phân tích dữ liệu thực nghiệm:} Kết quả từ Bảng \ref{table:thuc_nghiem_dot_huong} cho thấy sự thay đổi rõ rệt của chất lượng không khí qua ba pha. Tại pha Baseline (5 phút đầu), nồng độ các chất ô nhiễm duy trì ở mức thấp và ổn định. Giá trị PM2.5 trung bình đạt $\sim$19 $\mu$g/m$^3$, tương ứng với chỉ số AQI ở mức ``Tốt'' (màu xanh lá cây). Giá trị eCO$_2$ dao động quanh mức 442 ppm, nằm trong khoảng an toàn (như hiển thị trên giao diện Dashboard ở Hình \ref{fig:thuc_nghiem_baseline_dashboard}). Cùng với đó, danh sách nhật ký tổng hợp các thông số (Hình \ref{fig:thuc_nghiem_baseline_chart}) cho thấy dãy dữ liệu đo được duy trì ở trạng thái bình ổn với biên độ dao động rất nhỏ.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/thuc_nghiem_baseline_dashboard.jpg}
    \caption{Dashboard pha Baseline (trước khi đốt hương)}
    \label{fig:thuc_nghiem_baseline_dashboard}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/thuc_nghiem_baseline_chart.png}
    \caption{Nhật ký giám sát trong pha Baseline}
    \label{fig:thuc_nghiem_baseline_chart}
\end{figure}

Ngay sau khi bắt đầu đốt hương tại thời điểm 16:25, các chỉ số ô nhiễm gia tăng đột biến, thể hiện rõ ở pha Ô nhiễm. Do khói hương tỏa ra trực tiếp chứa số lượng lớn hạt bụi mịn, nồng độ PM2.5 tăng vọt từ 22 $\mu$g/m$^3$ lên đỉnh điểm 416 $\mu$g/m$^3$ chỉ trong vòng một phút (tại 16:26), tức là tăng gần 20 lần. Giá trị này quy đổi ra chỉ số AQI vượt mức ``Nguy hại'' (màu nâu đỏ), cảnh báo khẩn cấp tới sức khỏe (Hình \ref{fig:thuc_nghiem_onhiem_dashboard}). Bên cạnh đó, quá trình đốt cháy cũng sinh ra các khí độc hại, khiến nồng độ eCO$_2$ và eTVOC bắt đầu tăng kể từ phút thứ hai sau khi đốt. Sự thay đổi đồng loạt của tất cả các thông số (AQI, PM2.5, PM10, CO$_2$, TVOC) được theo dõi sắc nét thông qua danh sách đo lường cùng cấu trúc tương ứng với pha trước (Hình \ref{fig:thuc_nghiem_onhiem_chart}).

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/thuc_nghiem_onhiem_dashboard.jpg}
    \caption{Dashboard pha Đốt hương (AQI mức Nguy hại)}
    \label{fig:thuc_nghiem_onhiem_dashboard}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/thuc_nghiem_onhiem_chart.png}
    \caption{Nhật ký giám sát trong pha Ô nhiễm}
    \label{fig:thuc_nghiem_onhiem_chart}
\end{figure}

Tại pha Phục hồi (bắt đầu từ 16:33), thao tác dập tắt hương và mở cửa sổ đã tạo luồng đối lưu không khí, giúp các thông số ô nhiễm nhanh chóng giảm xuống. Qua sự quan trắc trên dữ liệu nhật ký của hệ thống (Hình \ref{fig:thuc_nghiem_phuchoi_chart}), ta có thể thấy các chỉ số đồng loạt khôi phục trạng thái an toàn. Nồng độ PM2.5 giảm mạnh từ 237 $\mu$g/m$^3$ xuống còn 41 $\mu$g/m$^3$ ở phút 16:37. Tương tự, eCO$_2$ cũng quay trở về sát mức nền là 405 ppm. Sự thay đổi nhạy bén và cập nhật liên tục này khẳng định hệ thống thu thập và hiển thị dữ liệu qua WebSocket chạy vô cùng ổn định với độ trễ cực thấp.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Hinhve/thuc_nghiem_phuchoi_chart.png}
    \caption{Nhật ký giám sát trong pha Phục hồi}
    \label{fig:thuc_nghiem_phuchoi_chart}
\end{figure}

\textbf{Kịch bản thử nghiệm thời gian sử dụng pin tối đa của Sensor Node:} Một trong những yêu cầu quan trọng khi triển khai Sensor Node ngoài trời là khả năng hoạt động bằng pin. Biến thể firmware \texttt{sensor-node-30pin-deepsleep} sử dụng chế độ deep-sleep của ESP32 để tối ưu năng lượng: sau mỗi chu kỳ đo và gửi dữ liệu, toàn bộ vi điều khiển chuyển sang trạng thái ngủ sâu (chỉ giữ RTC hoạt động), sau đó tự đánh thức theo bộ hẹn giờ để thực hiện chu kỳ tiếp theo.

\textbf{Thiết lập thử nghiệm:} Sensor Node sử dụng board ESP32 DOIT DevKit V1 (30 chân), pin 18650 Li-ion 3.7V dung lượng 1500 mAh, chu kỳ deep-sleep 30 phút. Mỗi chu kỳ thức, Sensor Node thực hiện tuần tự: khởi tạo phần cứng ($\sim$1.5s), đợi PMS7003 warm-up và đọc cảm biến ($\sim$30s), đọc pin và gửi gói LoRa ($\sim$1.5s) — tổng thời gian thức mỗi chu kỳ là $\sim$33 giây. Pin được sạc đầy 100\% trước khi bắt đầu thử nghiệm, Sensor Node hoạt động liên tục cho đến khi hết pin hoàn toàn (0\%, vi điều khiển không thể khởi động lại).

\textbf{Kết quả đo:} Bảng \ref{table:pin_thucnghiem} tóm tắt kết quả thử nghiệm.

\begin{table}[H]
\centering
\caption{Kết quả thử nghiệm thời gian sử dụng pin tối đa}
\label{table:pin_thucnghiem}
\small
\begin{tabularx}{\textwidth}{|c|X|}
\hline
\textbf{Thông số} & \textbf{Giá trị} \\
\hline
Board & ESP32 DOIT DevKit V1 (30 chân) \\
\hline
Pin sử dụng & 18650 Li-ion 3.7V / 1500 mAh \\
\hline
Chu kỳ deep-sleep & 30 phút \\
\hline
Thời điểm bắt đầu & 19/06/2026 09:30 \\
\hline
Thời điểm hết pin & 20/06/2026 06:00 \\
\hline
\textbf{Tổng thời gian hoạt động} & \textbf{20.5 giờ} \\
\hline
Số lần đo (ước tính) & $\sim$41 lần (20.5 giờ $\div$ 30 phút) \\
\hline
\end{tabularx}
\end{table}

% Bổ sung hình ảnh nếu có
% \begin{figure}[H]
%     \centering
%     \includegraphics[width=0.7\textwidth]{Hinhve/pin_test_setup.jpg}
%     \caption{Sensor Node hoạt động bằng pin trong thử nghiệm}
%     \label{fig:pin_test_setup}
% \end{figure}

\textbf{Nhận xét:} Kết quả thử nghiệm cho thấy với board DevKit V1 và pin 18650 dung lượng 1500 mAh, Sensor Node deep-sleep hoạt động liên tục được 20.5 giờ. Thời gian sử dụng pin bị hạn chế chủ yếu do các linh kiện trên board phát triển (chip CP2102 USB-UART bridge, bộ ổn áp AMS1117, đèn LED nguồn) tiêu thụ $\sim$26 mA ngay cả khi ESP32 ở chế độ deep-sleep. Để cải thiện, có thể sử dụng bare ESP32-WROOM module (giảm dòng deep-sleep xuống $\sim$1.5 mA) hoặc nâng dung lượng pin lên 3000--3500 mAh.

\textbf{Kịch bản thử nghiệm khoảng cách truyền LoRa tối đa:} Để đánh giá phạm vi phủ sóng thực tế của mạng LoRa 433 MHz, một thử nghiệm đo khoảng cách truyền tối đa được thực hiện giữa Sensor Node và Gateway trong không gian thoáng (outdoor, line-of-sight).

\textbf{Thiết lập thử nghiệm:} Gateway đặt cố định, anten gắn ở độ cao $\sim$1.5 mét so với mặt đất. Sensor Node được di chuyển dần ra xa trong không gian thoáng (không có vật cản lớn giữa hai thiết bị). Cả hai thiết bị sử dụng module LoRa AS32-TTL-100, tần số 433 MHz, anten mặc định đi kèm module. Tại vị trí xa nhất mà Gateway vẫn nhận được gói tin, ghi nhận khoảng cách và quan sát dữ liệu hiển thị trên server.

\textbf{Kết quả đo:} Bảng \ref{table:lora_range} tóm tắt kết quả thử nghiệm.

\begin{table}[H]
\centering
\caption{Kết quả thử nghiệm khoảng cách truyền LoRa tối đa}
\label{table:lora_range}
\small
\begin{tabularx}{\textwidth}{|c|X|}
\hline
\textbf{Thông số} & \textbf{Giá trị} \\
\hline
Module LoRa & AS32-TTL-100 (433 MHz) \\
\hline
Môi trường & Không gian thoáng (line-of-sight) \\
\hline
\textbf{Khoảng cách tối đa đạt được} & \textbf{800 mét} \\
\hline
Tầm phủ công bố của module & 3000 mét (không gian mở, điều kiện lý tưởng) \\
\hline
\end{tabularx}
\end{table}

% Bổ sung ảnh chụp vị trí thử nghiệm hoặc screenshot server nhận gói tin tại khoảng cách tối đa nếu có
% \begin{figure}[H]
%     \centering
%     \includegraphics[width=0.7\textwidth]{Hinhve/lora_range_test.jpg}
%     \caption{Thử nghiệm khoảng cách truyền LoRa tại vị trí xa nhất}
%     \label{fig:lora_range_test}
% \end{figure}

\textbf{Nhận xét:} Kết quả cho thấy trong điều kiện không gian thoáng, khoảng cách truyền LoRa tối đa đạt được là 800 mét, tương đương khoảng 27\% tầm phủ công bố của module (3000 mét trong điều kiện lý tưởng). Sự chênh lệch này là hợp lý do tầm phủ công bố được đo trong điều kiện lý tưởng (không nhiễu sóng, anten chuyên dụng, độ cao lớn), trong khi thử nghiệm thực tế chịu ảnh hưởng từ môi trường đô thị. Kết quả này xác nhận tính khả thi của mô hình Hub-Spoke: một Gateway đặt tại vị trí trung tâm có thể phục vụ nhiều Sensor Node trong bán kính 500--800 mét mà không cần hạ tầng mạng bổ sung. Trong thực tế triển khai tại khu dân cư (có vật cản như tường, cây xanh), khoảng cách hiệu quả sẽ giảm so với kết quả thử nghiệm này.

\subsection{So sánh với các hệ thống hiện có}
\label{subsection:4.5.2}

Bảng \ref{table:so_sanh_ket_qua} đối sánh hệ thống đề xuất với bốn hệ thống đã khảo sát ở Chương 2 (Bảng~\ref{table:so_sanh_he_thong}), bổ sung cột kết quả thực tế của hệ thống đã xây dựng.

\begin{table}[H]
\centering
\caption{So sánh hệ thống đề xuất với các hệ thống hiện có}
\label{table:so_sanh_ket_qua}
\small
\begin{tabularx}{\textwidth}{|X|c|c|c|c|X|}
\hline
\textbf{Tiêu chí} & \textbf{PAM Air} & \textbf{IQAir} & \textbf{PurpleAir} & \textbf{VN-AQI} & \textbf{Hệ thống đề xuất} \\
\hline
Cảm biến & PM2.5 & PM2.5, CO\textsubscript{2} & PM2.5, PM10 & Đầy đủ (FEM) & PM2.5, PM10, CO\textsubscript{2}, TVOC, T, H \\
\hline
Truyền thông & WiFi/4G & WiFi & WiFi & Chuyên dụng & LoRa + WiFi \\
\hline
Chi phí/trạm & Trung bình & Cao & Trung bình & Rất cao & Thấp \\
\hline
Mã nguồn mở & Không & Không & Một phần & Không & Có (toàn bộ) \\
\hline
Tuỳ biến & Không & Không & Hạn chế & Không & Có \\
\hline
LoRa & Không & Không & Không & Không & Có (433 MHz) \\
\hline
Realtime web & Có & Có & Có & Hạn chế & Có (Socket.IO) \\
\hline
Admin panel & Không rõ & Không & Không & Có (nội bộ) & Có (web) \\
\hline
\end{tabularx}
\end{table}

Kết quả so sánh cho thấy hệ thống đề xuất giải quyết được bốn hạn chế chính đã xác định ở Chương 2. Thứ nhất, hệ thống đo đa thông số (sáu thông số) trong khi PAM Air và PurpleAir chỉ đo bụi mịn. Thứ hai, truyền thông LoRa cho phép triển khai Sensor Node tại các vị trí không có WiFi, khác biệt so với tất cả bốn hệ thống khảo sát đều yêu cầu WiFi hoặc mạng di động cho mỗi trạm. Thứ ba, mã nguồn mở hoàn toàn cho phép tuỳ biến và mở rộng, trong khi các hệ thống thương mại không hỗ trợ. Thứ tư, chi phí mỗi trạm Sensor Node thấp nhờ sử dụng linh kiện phổ thông và module LoRa giá rẻ. Tuy nhiên, hệ thống đề xuất có hai hạn chế so với VN-AQI: độ chính xác của cảm biến chi phí thấp không đạt tiêu chuẩn FEM, và quy mô triển khai hiện tại còn nhỏ (vài trạm thử nghiệm).

Bảng \ref{table:bom_cost} trình bày chi phí linh kiện chi tiết cho một bộ Sensor Node và một bộ Gateway.

\begin{table}[H]
\centering
\caption{Chi phí linh kiện (BOM) cho Sensor Node và Gateway}
\label{table:bom_cost}
\small
\begin{tabularx}{\textwidth}{|c|X|c|c|c|}
\hline
\textbf{STT} & \textbf{Linh kiện} & \textbf{Số lượng} & \textbf{Đơn giá (VNĐ)} & \textbf{Thành tiền} \\
\hline
\multicolumn{5}{|l|}{\textit{Sensor Node}} \\
\hline
1 & ESP32 DevKit V1 (30 chân) & 1 & 100.000 & 100.000 \\
\hline
2 & Module LoRa AS32-TTL-100 & 1 & 120.000 & 120.000 \\
\hline
3 & Cảm biến bụi PMS7003 & 1 & 350.000 & 350.000 \\
\hline
4 & Cảm biến CCS811 (eCO\textsubscript{2}, eTVOC) & 1 & 150.000 & 150.000 \\
\hline
5 & Cảm biến AHT10 (nhiệt độ, độ ẩm) & 1 & 35.000 & 35.000 \\
\hline
6 & Màn hình OLED SSD1306 0.96'' & 1 & 40.000 & 40.000 \\
\hline
7 & Pin 18650 Li-ion 3.7V & 1 & 30.000 & 30.000 \\
\hline
8 & Dây nối, mạch in, điện trở & 1 & 50.000 & 50.000 \\
\hline
\multicolumn{4}{|r|}{\textbf{Tổng chi phí Sensor Node}} & \textbf{875.000} \\
\hline
\multicolumn{5}{|l|}{\textit{Gateway}} \\
\hline
9 & ESP32 DevKit V1 (30 chân) & 1 & 100.000 & 100.000 \\
\hline
10 & Module LoRa AS32-TTL-100 & 1 & 120.000 & 120.000 \\
\hline
11 & Màn hình OLED SSD1306 0.96'' & 1 & 40.000 & 40.000 \\
\hline
12 & Dây nối, mạch in, nguồn 5V & 1 & 50.000 & 50.000 \\
\hline
\multicolumn{4}{|r|}{\textbf{Tổng chi phí Gateway}} & \textbf{310.000} \\
\hline
\end{tabularx}
\end{table}

Tổng chi phí linh kiện cho một trạm đo hoàn chỉnh (1 Sensor Node + 1 Gateway) là khoảng 1.185.000 VNĐ ($\sim$48 USD), thấp hơn đáng kể so với các giải pháp thương mại như IQAir 10.000.000 VNĐ($\sim$400 USD). Nếu một Gateway phục vụ nhiều Sensor Node (mô hình Hub-Spoke), chi phí trung bình mỗi điểm đo còn giảm thêm.

Chương này đã trình bày toàn bộ quá trình thiết kế, xây dựng, kiểm thử và phân tích kết quả vận hành của hệ thống giám sát chất lượng không khí. Các kết quả chính đạt được bao gồm: (i) về thiết kế, hoàn thiện kiến trúc bốn tầng kết hợp mô hình Hub-Spoke với mã nguồn được tổ chức theo nguyên tắc phân lớp rõ ràng; (ii) về xây dựng, hiện thực hóa đầy đủ chín chức năng và năm yêu cầu phi chức năng (Bảng~\ref{table:req_mapping}); (iii) về kiểm thử, hoàn thành 44 kịch bản kiểm thử (25 thủ công và 19 tự động) với tỷ lệ đạt 100\%; (iv) về hiệu năng và độ tin cậy, hệ thống đạt thời gian phản hồi API dưới 3s, duy trì uptime tuyệt đối trong 7 ngày liên tục và phát hiện thiết bị mất kết nối chuẩn xác; (v) về thực nghiệm, cảm biến mức độ phản hồi nhạy bén trong môi trường ô nhiễm, Sensor Node duy trì hoạt động liên tục 20.5 giờ (Bảng~\ref{table:pin_thucnghiem}), truyền thông LoRa đạt khoảng cách truyền tối đa 800 mét (Bảng~\ref{table:lora_range}), đồng thời tối ưu chi phí phần cứng ở mức cạnh tranh khoảng 875.000 VNĐ cho mỗi trạm đo. Những kết quả này chứng minh rõ nét tính khả thi của cấu trúc giám sát đa thông số chi phí thấp sử dụng mạng thông tin vô tuyến LoRa. Thừa hưởng các đánh giá thực chứng đó, Chương 5 sẽ phân tích tổng hợp các đóng góp nổi bật và định hướng phát triển của hệ thống trong tương lai.

\end{document}
